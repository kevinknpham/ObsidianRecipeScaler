# Obsidian Recipe Scaler

A simple plugin for my recipe books stored in Obsidian.  It scans through code blocks for recipe measurements and scales them up based on a scale factor set in the plugin settings.
